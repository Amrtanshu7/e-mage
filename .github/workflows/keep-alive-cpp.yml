name: Keep C++ Service Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping C++ health endpoint
        run: |
          curl -fsS https://emage-cpp.onrender.com/health || echo "CPP not reachable"
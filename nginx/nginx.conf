events {}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile on;
    keepalive_timeout 65;

    server {
        listen 80;
        server_name localhost;

        # ---- Frontend ----
        location / {
            proxy_pass http://frontend:80;
            proxy_http_version 1.1;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # ---- Backend API ----
        location /api/ {
            rewrite ^/api/?(.*)$ /$1 break;

            proxy_pass http://backend:8080;
            proxy_http_version 1.1;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # ==================================================
    # HTTPS BLOCK (will be enabled AFTER deployment)
    # ==================================================
    #
    # server {
    #     listen 443 ssl;
    #     server_name emage.yourdomain.com;
    #
    #     ssl_certificate /etc/letsencrypt/live/emage.yourdomain.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/emage.yourdomain.com/privkey.pem;
    #
    #     location / {
    #         proxy_pass http://frontend:80;
    #     }
    #
    #     location /api/ {
    #         rewrite ^/api/?(.*)$ /$1 break;
    #         proxy_pass http://backend:8080;
    #     }
    # }
}
